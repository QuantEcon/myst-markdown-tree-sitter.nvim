-- myst-markdown-tree-sitter.nvim
-- MyST Markdown plugin for Neovim with Tree-sitter support
--
-- This file is sourced automatically by Neovim. It only sets the load guard.
-- Users must call require("myst-markdown").setup() in their config to
-- initialise the plugin. This allows configuration to be passed before any
-- autocmds or commands are created.

if vim.g.loaded_myst_markdown then
  return
end
vim.g.loaded_myst_markdown = 1
